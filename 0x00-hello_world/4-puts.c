#include <stdio.h>
/**
 *  * main-Aprogram that prints a line using puts function
 *   * Return: 0 (success)
 *    */
int main(void)
{
		/string initialisation/
			char Mystr[] = "\"Programming is like building a multilingual puzzle";

		puts(Mystr); /writing the string to stdout/

			return (0);
}
